제공해주신 이미지의 내용을 바탕으로 마크다운(Markdown) 문서를 작성해 드립니다. 이미지에 있는 텍스트를 최대한 정확하게 옮기고 가독성 있게 정리했습니다.

---

# 5. 평가문항

## 항목 1

* (옵션 A/B) 저장소가 팀 단위로 1개이며, 팀원이 협업 가능한 권한으로 참여했는가?
* `main` 브랜치에 Branch Protection이 설정되어 있고, 직접 `push` 없이 PR로만 병합되었는가?
* 각 PR이 이슈와 연동되어 있으며 (예: `Closes #n`), 추적 가능한가?
* 팀원별로 PR 2개 이상이 병합되어 있고, 팀원별 PR 링크를 `SUBMISSION.md`에서 확인할 수 있는가?
* 팀원별로 리뷰 2개 이상이 존재하고, 각 팀원이 최소 1회는 본인 PR에서 리뷰를 반영했는가?
* 충돌 해결 기록이 최소 2회 존재하며, 그 중 1회는 비자명 충돌 기준을 충족하는가?
* 트러블슈팅 4종(`amend/reset/revert/stash`)이 모두 수행되었고, 팀원별로 최소 1개 시나리오 기록에 참여했는가?
* `docs/CONTRIBUTING.md`, `docs/conflict-resolution.md`, `docs/troubleshooting-log.md`가 존재하고 템플릿 수준의 구조를 갖추었는가?
  * *(우측 선택 버튼: `PASS` / `FAIL`)*

## 항목 2

* 브랜치 전략을 "작업 단위"로 나누는 기준을 어떻게 정했는지 설명할 수 있는가?
* PR 본문에 `What/Why/How`와 이슈 연동 정보를 일관되게 남기기 위해 어떤 규칙/도구(템플릿 등)를 정했는지 설명할 수 있는가?
* 리뷰 코멘트가 "LGTM"에 그치지 않도록 팀에서 적용한 최소 품질 기준과 운영 방법을 설명할 수 있는가?
* 충돌 발생 시, 팀이 어떤 흐름(공유→해결→기록)으로 대응했는지 설명할 수 있는가?
* 트러블슈팅 로그를 "재현 가능"하게 만들기 위해 어떤 항목(상황/절차/결과/주의점)을 고정했는지 설명할 수 있는가?
  * *(우측 선택 버튼: `PASS` / `FAIL`)*

## 항목 3

* GitHub Flow에서 `main`을 "항상 배포 가능 상태"로 유지해야 하는 이유를 설명할 수 있는가?
* `main`에 직접 `push`하지 않고 PR + 승인으로 병합하는 이유(품질/책임/추적성)를 설명할 수 있는가?
* 이슈-PR 연동을 하는 이유(추적성/커뮤니케이션/자동화)를 설명할 수 있는가?
* 원격에 `push`된 커밋을 되돌릴 때 `reset` 대신 `revert`를 선택해야 하는 이유를 설명할 수 있는가?
* 충돌 마커(`<<<<<<<`, `=======`, `>>>>>>>`)의 의미와, 비자명 충돌을 해결할 때 고려한 의사결정 기준을 설명할 수 있는가?
  * *(우측 선택 버튼: `PASS` / `FAIL`)*

## 항목 4

* 만약 `main`에 긴급 핫픽스가 필요하다면, GitHub Flow에서 어떤 순서로 처리할지(브랜치/PR/리뷰/배포) 설명할 수 있는가?
* 만약 팀원이 실수로 "의미없는 커밋 메시지"를 여러 개 `push`했다면, 협업 중단 없이 개선하는 방법을 설명할 수 있는가?
* 만약 충돌이 반복적으로 발생한다면(같은 파일/같은 영역), 원인 분석과 예방 방안을 제출할 수 있는가?
* (보너스 수행 시) 왜 리베이스가 히스토리를 깨끗하게 만들지만 협업에는 위험할 수 있는지, 안전 수칙을 포함해 설명할 수 있는가?
  * *(우측 선택 버튼: `PASS` / `FAIL`)*

---
**Branch명:** `main`